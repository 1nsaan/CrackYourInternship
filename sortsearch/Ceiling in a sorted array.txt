int  nums[];
int ceil=Integer.MIN_VALUE;
int floor = Integer.MAX_VALUE;

for(int i=0;i<nums.length;i++){
       if(nums[i]>=x && nums[i]<ceil) ceil = nums[i];
       if(nums[i]<=x && nums[i]>floor) floor= nums[i];
}

return new int[]{floor, ceil};